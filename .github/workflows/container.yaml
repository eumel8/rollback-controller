name: Build Docker Container (template)
permissions:
  attestations: write
  contents: read
  id-token: write
  packages: write

on:
  push:

jobs:
  call-workflow:
    uses: eumel8/workflow-templates/.github/workflows/container.yaml@0.0.10
